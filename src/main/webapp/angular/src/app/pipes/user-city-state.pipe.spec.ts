import { UserCityStatePipe } from './user-city-state.pipe';

describe('UserCityStatePipe', () => {
  it('create an instance', () => {
    const pipe = new UserCityStatePipe();
    expect(pipe).toBeTruthy();
  });
});
